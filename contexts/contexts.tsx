// import { Dispatch, SetStateAction, createContext } from "react";

// export const SectContext = createContext(
// 	[] as [string?, Dispatch<SetStateAction<string>>?]
// );

// type Props = {
// 	children: React.ReactNode;
// 	value: [string?, Dispatch<SetStateAction<string>>?];
// };

// function Contexts({ children, value }: Props) {
// 	return <SectContext.Provider value={value}>{children}</SectContext.Provider>;
// }

// export default Contexts;
